import React from "react";
import ReactDOM from "react-dom";
import CinepolisApp from "./CinepolisApp";
import "../src/styles/styles.scss";

ReactDOM.render(
  <React.StrictMode>
    <CinepolisApp />
  </React.StrictMode>,
  document.getElementById("root")
);
